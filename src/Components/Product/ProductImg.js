import React from 'react';
import imageUrl from './container-product-list/ContainerProductList';

const ProductImg = () => {
  return <img key={imageUrl} src={imageUrl} alt=""/>
};

export default ProductImg;
